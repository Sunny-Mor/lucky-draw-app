# This file is a TEMPLATE. The actual secret is created by the deploy workflow
# using values from GitHub Actions secrets. Do NOT commit real values here.
#
# The deploy workflow runs:
#   kubectl create secret generic app-secret \
#     --from-literal=ADMIN_USERNAME=${{ secrets.ADMIN_USERNAME }} \
#     --from-literal=ADMIN_PASSWORD=${{ secrets.ADMIN_PASSWORD }} \
#     --from-literal=SECRET_KEY=${{ secrets.APP_SECRET_KEY }} \
#     --namespace=luckydraw --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: luckydraw
type: Opaque
data:
  ADMIN_USERNAME: REPLACE_BASE64   # echo -n 'admin' | base64
  ADMIN_PASSWORD: REPLACE_BASE64   # echo -n 'your-password' | base64
  SECRET_KEY: REPLACE_BASE64       # echo -n 'your-flask-secret' | base64
